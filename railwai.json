{
    "build": {
      "builder": "nixpacks",
      "config": {
        "startCommand": "php-fpm -y /etc/php/php-fpm.conf && nginx -c /app/nginx.conf",
        "webCommand": "./public"
      }
    }
  }